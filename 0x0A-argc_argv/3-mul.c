#include "main.h"
#include <stdio.h>

/**
 * main - multiplies two number
 * @z: argument count
 * @y: character of pointers
 * Return: 0
 */

int main(int z, char *argv y[])
{
	atoi(y) = ((int)a * (int)b);

	printf("%d\n", y);

	return (0);
}
